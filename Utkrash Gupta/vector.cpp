#include<bits/stdc++.h>
using namespace std;

int main(){
    // vector<int>v;
    // v.push_back(5);
    // v.push_back(6);
    // v.push_back(7);

    // when you define a vector without size it will be empty

    // cout<<v.size()<<'\n';

    // cout<<v[0]<<" "<<v[1]<<" "<<v[2]<<'\n';

    // v.pop_back();

    // cout<<v.size()<<'\n';
    

    // v.push_back(2);

    // cout<<v.size()<<'\n';

    // v.clear();

    // cout<<v.size()<<'\n';
    

    // cout<<v[0]<<" "<<v[1]<<" "<<v[2]<<'\n';


    // vector<int>v(50); // {0,0,0,0,0,.........}
    // vector<int>v(50,7); // {7,7,7,7,........}

    // cout<<v[23]<<'\n';

    

    return 0;
}